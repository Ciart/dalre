import styles from './status-bar.module.css';

export const StatusBar = () => {
  return (
    <div className={styles['container']}>
      <div className={styles['item']}>StatusBar</div>
    </div>
  );
};
