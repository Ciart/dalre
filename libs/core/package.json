{
  "name": "@dalre/core",
  "type": "module",
  "module": "index.js",
  "scripts": {
    "build": "rimraf dist pkg && webpack",
    "serve": "rimraf dist pkg && webpack-dev-server -d eval-source-map",
    "test": "cargo test && wasm-pack test --headless"
  }
}
